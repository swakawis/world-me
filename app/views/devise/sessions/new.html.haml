.AccountPage
  .AccountPage__credit
    = link_to '画像出典: nasa.gov', "https://www.nasa.gov/sites/default/files/thumbnails/image/16873156139_f2493e3b50_k.jpg"
  .AccountPage__logo
    WorldMe
  .AccountPage__message
    情報は多ければ多いほど
    %br 選択肢＝未来が広がる
  .AccountPage__title
    = render "devise/shared/links"
    %br
    %h2 ログインをしてください
  .AccountPage-form
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .FormField
        .FormField__label--normal
          = f.label :email
        .FormField__input--normal
          = f.email_field :email, autofocus: true
        .FormField
          .FormField__label--normal
            = f.label :password
            %i (英数字8文字以上)
          .FormField__input--normal
            = f.password_field :password, autocomplete: "off"
        .actions
          = f.submit "Log in", class: 'Button UserButton' 